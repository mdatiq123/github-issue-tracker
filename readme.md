# questions and answers

## 1. What is the difference between var, let, and const?

### ans : var is global scope variable , we can  Redeclare same variable name by VAR keyWord and it hoisting and value initialized as undefined .
### let is block scoped variable ,we can change it's value but can not Redeclare it .
### const is block scoped variable ,we can not reassign it's value .

## 2. What is the spread operator (...)?
### ans : spreed operator is  built-in method that give separate value of array and object without changing original array or object . 
## 3.What is the difference between map(), filter(), and forEach()?
### ans : map() , filter() and forEach() are built-in loop method .these three use for loop .
### map() - takes an array and modifies every single element and return a new array.
### filter() - it check the condition .if the condition is true it take them it into new array . it kick the false condition elements .
### forEach() - it is a simple loop method .it doesn't give any return value .

## 4. What is an arrow function?
### ans : arrow function works like regular function .it has a little difference between arrow and regular function .in arrow function we don't need to write function keyword in a variable we can write a arrow function ,after perams () we must use => arrow sign to declare a arrow function .  

## 5. What are template literals?
### ans : template literals is a powerful feature of JS .we can inject variables in template literals string by using ${variable-name} syntax .